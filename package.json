{
  "name": "statistic-service",
  "version": "1.0.0",
  "description": "Statistics microservice for URL shortener - retrieves analytics data for shortened URLs",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "npm install --production && zip -r function.zip src/ node_modules/"
  },
  "keywords": [
    "url-shortener",
    "statistics",
    "analytics",
    "lambda",
    "dynamodb"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.679.0",
    "@aws-sdk/lib-dynamodb": "^3.679.0"
  }
}
